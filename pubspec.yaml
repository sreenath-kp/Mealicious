name: meals
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.3.6
  google_fonts: ^4.0.5
  transparent_image: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  image_path: "assets/mealsapp.png"
  adaptive_icon_background: "#30DDCF"
  adaptive_icon_foreground: "assets/mealsapp.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icons/
